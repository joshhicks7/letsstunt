/** Max members shown in group roster grid (detail + future cards). */
export const GROUP_MEMBER_MAX = 4;

/**
 * On Discover, group cards only cycle through this many photo slots (0-based depth)
 * per member in the quadrant; full galleries stay on the group profile screen.
 */
export const DISCOVER_GROUP_MAX_PHOTO_SLOTS = 4;
