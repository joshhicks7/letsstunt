import type { Href } from 'expo-router';

/** Route to Terms of Service (`app/(auth)/terms.tsx`). Cast until typed routes pick up new files. */
export const hrefTermsOfService = '/(auth)/terms' as Href;
